<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="28dp"
        android:paddingEnd="28dp"
        android:layout_marginTop="36dp">

        <!-- Existing ImageView from the second layout -->
        <!-- You can adjust the layout parameters as needed -->
        <ImageView
            android:id="@+id/img"
            android:layout_width="300dp"
            android:layout_height="300dp"
            android:layout_centerHorizontal="true"
            android:layout_centerVertical="true"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="32dp"
            android:scaleType="centerCrop" /> <!-- placeholder image -->

        <!-- Remaining contents from the second layout -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="24dp">

            <!-- First Button -->
            <Button
                android:id="@+id/downloadButton"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_gravity="center_horizontal"
                android:text="Download"
                android:textAllCaps="false"
                android:textSize="16sp"
                android:background="@drawable/rounded_button"
                android:clickable="true"
                android:focusable="true"
                android:elevation="30dp"
                android:padding="12dp"
                android:layout_weight="1"/>

            <!-- Add margin between the two buttons -->
            <Space
                android:layout_width="8dp"
                android:layout_height="match_parent" />

            <!-- Second Button -->
            <Button
                android:id="@+id/shareButton"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_gravity="center_horizontal"
                android:text="Share"
                android:textAllCaps="false"
                android:textSize="16sp"
                android:clickable="true"
                android:background="@drawable/rounded_corners"
                android:focusable="true"
                android:elevation="30dp"
                android:padding="12dp"
                android:layout_weight="1"/>
        </LinearLayout>
    </LinearLayout>
</RelativeLayout>
